# 護法會頒證出席報到系統－安裝與設定指南（v2.0：前後端分離版）

適用架構：Google Apps Script（後端 API）＋ GitHub Pages（前端靜態網頁）

> 此版本解決了 v1.0（Apps Script 內建網頁）在部分手機瀏覽器上「相機一直無法開啟」的問題。
> 原因是 Apps Script 的 HtmlService 頁面會被包在一層 Google 的 iframe 沙盒中，
> 有些瀏覽器（尤其手機版）不允許被包在 iframe 裡的頁面要求相機權限。
> 改為獨立網域的靜態頁面（GitHub Pages）後，就是一般網頁，相機權限請求會正常運作。

## 一、後端：部署 Apps Script API

1. 前往 https://script.google.com ，新增專案，命名為「護法會頒證出席報到系統」。
2. 刪除預設 `Code.gs` 內容，貼上本次交付的 `Code.gs`（v2.0，已改為 API 版本，不需要再建立 Index/Admin 等 HTML 檔案）。
3. 儲存後，點選右上角「部署」→「新增部署作業」→ 類型選「網頁應用程式」：
   - 執行身分：**我**
   - 具有存取權的使用者：**知道連結的任何人**
4. 部署後跳出的授權畫面請允許存取 Google Sheet。
5. 記下部署完成後取得的網址，格式類似：
   `https://script.google.com/macros/s/xxxxxxxxxxxx/exec`
   這組網址就是後續 `checkin.html`、`admin.html` 中要填入的 `GAS_API_URL`。

> 之後若修改 `Code.gs`，記得「管理部署作業」→ 編輯 → 版本選「新版本」→ 部署，網址才會套用最新程式碼。

## 二、準備出席名單 Google Sheet

（與 v1.0 相同，若已設定過可略過）

表頭（第一列）需包含：`學員編號`、`報到`、`報到時間`（報到前保持空白）；
建議保留 `姓名`、`法名`、`組別`、`新任聘書`；選填 `注意事項`（個人專屬提醒）。

## 三、前端：設定並發布到 GitHub Pages

1. 打開 `checkin.html` 與 `admin.html`，找到這一行：
   ```js
   var GAS_API_URL = 'https://script.google.com/macros/s/請貼上您的部署ID/exec';
   ```
   把網址換成第一步取得的部署網址，兩個檔案都要改。
2. 將 `checkin.html`、`admin.html`、`README.md` 這個資料夾整個推送到 GitHub：
   - 在 GitHub 建立一個新的 repository（例如 `jingshe-checkin`），可設為 Private 或 Public 皆可（Public 才能用免費的 GitHub Pages；Private 則需 GitHub Pro 方案才能啟用 Pages）。
   - 用 GitHub Desktop 或指令列將本資料夾的內容加入該 repository 並推送（push）。
3. 到該 repository 的 **Settings → Pages**：
   - Source 選擇 `Deploy from a branch`
   - Branch 選擇 `main`（或您推送的分支）／資料夾選 `/ (root)`
   - 儲存後，GitHub 會給您一個網址，格式類似：
     `https://您的帳號.github.io/repo名稱/checkin.html`
     `https://您的帳號.github.io/repo名稱/admin.html`
4. 開啟這兩個網址測試：`admin.html` 先設定場次，`checkin.html` 測試相機是否能正常開啟、掃描是否成功報到。

## 四、日常操作

1. 管理人員開啟 `.../admin.html`，新增／編輯／切換場次（與 v1.0 操作方式相同）。
2. 報到台開啟 `.../checkin.html`，允許相機權限後即可開始掃描報到。
3. 若相機仍無法開啟，請確認：
   - 網址是否為 `https://`（GitHub Pages 預設就是 https，一般不會有問題）
   - 瀏覽器是否曾經「封鎖」過相機權限（可到瀏覽器設定裡針對該網站重新允許）
   - 是否使用內嵌瀏覽器開啟（例如 LINE、Facebook 內建瀏覽器有時會限制相機權限），建議改用系統預設瀏覽器（Chrome／Safari）開啟

## 五、常見問題

| 問題 | 排查方式 |
| --- | --- |
| 報到頁顯示「尚未設定 GAS_API_URL」 | 確認 `checkin.html`／`admin.html` 內的 `GAS_API_URL` 已換成正確的部署網址 |
| 報到頁顯示「連線失敗」 | 確認 Apps Script 已部署為「知道連結的任何人」，且部署網址正確、未過期 |
| 掃描後顯示「查無此人」 | 確認出席名單中的「學員編號」與QR Code內容是否一致（含前導0、空白等） |
| 多台裝置同時報到偶爾變慢 | 系統已使用 LockService 排隊處理寫入，屬正常現象 |
| 需要換一場活動 | 開啟 `admin.html` 新增或切換場次，不需重新部署 Apps Script |
| GitHub Pages 網址打不開 | 確認 Settings → Pages 已啟用，且等待 1～2 分鐘讓部署生效 |

## 六、維護與擴充建議

- 每次修改 `checkin.html`／`admin.html` 後，重新 `git commit` + `git push` 即可，GitHub Pages 會自動更新（約 1 分鐘生效）。
- 每次修改 `Code.gs` 後，記得到 Apps Script「管理部署作業」建立新版本，否則舊網址仍執行舊程式碼。
- 若要新增報到欄位，出席名單新增欄位後，於 `Code.gs` 的「執行報到」函式對應增加讀寫邏輯即可。
